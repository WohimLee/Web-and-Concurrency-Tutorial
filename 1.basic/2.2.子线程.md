
## 子线程

主线程和子线程的关系可以记成：

1. 主线程是进程启动时默认创建的第一个线程。  
2. 子线程由主线程（或其他线程）创建出来，属于同一个进程。  
3. 它们共享同一进程资源：内存、文件句柄、全局变量。  
4. 每个线程也有自己的执行栈和寄存器上下文。  
5. 主线程可以 `join()` 等待子线程结束，也可以不等。  
6. 进程是否退出通常受“是否还有非守护线程存活”影响。  

一句话：主线程和子线程是“同一进程内并行执行的兄弟关系”，不是父子进程那种资源隔离关系。