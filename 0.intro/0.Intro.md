## Intro

>适合人群
- 有调用大模型 API 经验
- 有 Python 基础


>课程配置
- IDE: VSCode
- Python 环境管理: Astral UV


async def call_llm():
    client = OpenAI()
    resp = client.chat.completions.create(model="gpt-4o-mini", messages=[...])
    return resp