# 03 交互控件与表单

## 常用控件

- 文本输入：`st.text_input`、`st.text_area`
- 数值输入：`st.slider`、`st.number_input`
- 选择：`st.selectbox`、`st.multiselect`、`st.radio`
- 上传：`st.file_uploader`

## 为什么要用表单

默认情况下，控件变化会触发重跑。多个参数联动时，用 `st.form` 可以“最后一次性提交”，减少反复计算。

## 本章代码

- `src/03_widgets_and_form.py`
