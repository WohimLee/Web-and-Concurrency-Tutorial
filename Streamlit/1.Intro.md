# Intro

## 核心概念

- Streamlit 是“脚本式 Web 框架”。
- 用户每次交互，脚本会自上而下重跑。
- 不是 Flask/Django 的 request-response 模型。

## 最小程序

```python
import streamlit as st
st.title("Hello Streamlit")
st.write("第一个页面")
```

## 运行命令

```bash
streamlit run src/01_hello_streamlit.py
```

