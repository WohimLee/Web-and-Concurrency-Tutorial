# 02 组件与布局

## 重点组件

- 文本：`st.title`、`st.header`、`st.code`
- 数据：`st.table`、`st.dataframe`
- 媒体：`st.image`
- 布局：`st.sidebar`、`st.columns`、`st.expander`、`st.tabs`

## 布局建议

1. 过滤条件放 `sidebar`。
2. 关键指标放第一屏 `metric`。
3. 详情内容放 tabs 或 expander。

## 本章代码

- `src/02_text_media_layout.py`
